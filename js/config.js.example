let config = {
    path: "data.geojson",
    field: ["name"], // attribute(s) to use for comparison
    basemap: false,
    retriesPerItem: 2, // set to -1 for infinity
    colors: {
        current: "#53777A",
        right: "green",
        wrong: "red",
        selected: "yellow"
    },
    timer: {
        position: "topright", // or boolean
        timeout: 5000 // milliseconds - set -1 for infinity
    }
};